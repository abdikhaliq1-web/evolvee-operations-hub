/**
 * Evolvée Radiance — Partner referral attribution for Shopify
 *
 * NOT CONNECTED YET — this script is ready but should only be installed on the
 * Shopify theme once PLACEHOLDER_SHOPIFY_* values in .env have been replaced
 * with the actual Evolvée Radiance store integration.
 *
 * When ready:
 * 1. Replace PLACEHOLDER_SHOPIFY_STORE_LINK in .env with the real store URL
 * 2. Upload this file to Shopify theme Assets
 * 3. Add to theme.liquid before </body>:
 *    <script src="{{ 'partner-attribution.js' | asset_url }}" defer></script>
 */
(function () {